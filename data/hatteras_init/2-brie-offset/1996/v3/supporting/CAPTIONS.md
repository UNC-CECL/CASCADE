# Captions — v3

Written by the figure scripts through `hat_figure_style.caption()`; the images carry no titles or footnotes, this file does.

**`offset_1996_v2_vs_v3.png`.** Two builds of the 1996 island offset. (a) The unpadded offset each build hands the model, v2 in red and v3 in blue, each zeroed on its own most seaward domain. (b) The change in the dune line itself, v3 minus v2, measured from the shared offshore datum along the 100 m transects and averaged per 500 m domain; positive is landward. 32 of 90 domains differ by 0.5 m or more (mean over all domains -7.8 m; the largest, +66.2 m, at GIS 35). Both raw files were produced by the same shapely intersection, so the metre-scale station convention of the earlier ArcGIS export is not part of the difference.
