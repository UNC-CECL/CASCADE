`RoadSetback_1984_dunestart.csv`: the 1984 setbacks from `2-domain-reconstruction-1984/2-extent/footprint_1984_by_domain.csv` (`setback_new_m` = (road - row 0) + the paired 1997-1984 dune-line shift, per profile, median per domain), in the two-row model-facing format copied from `v2`'s file. No value is negative, so nothing is floored. Domains the footprint has no setback for keep `v2`'s value: [].
