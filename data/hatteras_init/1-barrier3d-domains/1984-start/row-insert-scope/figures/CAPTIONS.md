# Figure captions

Written by `HAT_footprint_1984.py` (`write_captions`) and by hand for the figures of the other two scripts. Figures live under `2-footprint-1984/` (root, `seaward/`, `behind-road/`) and `3-fill/`; README.md maps them. The figures carry no in-image titles or footnotes on purpose; use these under them.

## `HAT_row_insert_grid.png`

(a–c) The 90 Barrier3D domains as the model would hold them, 50 alongshore cells each, every cross-shore row down the page from the two dune rows (right axis in metres; interiors run to 189 rows, white is off the array, cells are not square). Existing cells are shaded by elevation class (m above MHW), so the dune ridge, the backbarrier flat and the sound-side marsh are distinguishable. Where the 1984 dune line lay seaward of the 1997 line, N blank rows (red) are added between the dune and the existing interior, which is pushed down the page; where it lay landward, the existing rows 0 to |N|−1 are hatched (blue) and the interior the model would hold starts below them; the signed count is printed above each changed domain. The dark bar is NC-12 at its measured 1984 position, 20 m wide; it moves down with the interior where rows are added and stays put where rows are removed. Communities and villages along the bottom of each panel, from the site configuration. 29 domains gain 73 rows, 23 lose 47, 38 are unchanged.

## `HAT_row_insert_grid_behindroad.png`

(a–c) As `HAT_row_insert_grid.png`, with the same rows placed directly BEHIND THE ROADWAY ROWS instead of at the seaward edge. The roadway in the model is two straight rows at one setback per domain, so the block goes in at int(setback / 10) + 2 rows from interior row 0 with the road at its 1984 setback (2026-09-08), and the strip from the dune crest through the road is kept exactly as measured; the added rows (red, blank) push only the backbarrier down the page, and removals (blue hatching) take backbarrier rows at the same index. The dark bar is NC-12 where the model places it under the 1984 setback (at GIS 85 rows 4–5, the block starting at row 6); the measured pavement rows are outlined. Row 0 and the dune do not move. Domains without a model road (GIS 1–5, 8) take the crest row as the anchor instead: the block goes in one row behind it. The insert row per domain is `insert_row_behind_road` in `footprint_1984_by_domain.csv`. N is identical to the seaward placement: 29 domains gain 73 rows, 23 lose 47, 38 are unchanged.

## `HAT_where_inserts_occur_blocks.png`

(a) The inter-village NC-12 relocation block, GIS 9–14, and (b) the Pea Island block, GIS 84–87: per domain, the median of the 50 paired per-profile differences between the 1997 and 1984 dune-line crossings (points, p10–p90), positive where the 1984 line lies seaward, and the rows the 10 m rule keeps of it (bars, N × 10 m; red added, blue removed, grey unchanged), labelled with the signed row count. Dashed guides at ±1 cell. A green triangle marks N if the 13-year measurement were scaled to the 12-year 1984–1996 interval; recorded, not applied.

## `HAT_where_inserts_occur_setback.png`

The NC-12 setback at the ten relocation-block domains, in metres landward of interior row 0: as the model receives it today (grey; the v2 measurement floored at 0, so GIS 85 and 86 stand at 0) and from the new row 0 (coloured by what happens to the domain, with the p10–p90 over the 50 profiles), values printed above each bar. The new value is (road − row 0) + shift per profile, then the median, unrounded.

## `HAT_fill_copy_grid_GIS<N>.png` (3-fill; GIS 80, 85, 5, 49)

The copy fill for the rows placed behind NC-12, at one domain. (a) The interior as extracted (v2), 50 alongshore cells across, cross-shore rows down the page from interior row 0, shaded by elevation class (m above MHW); the model's two roadway rows as a dark band labelled NC-12, and the source window, the N rows immediately landward of the insert point, dashed in red. (b) The built version v3: the N-row block inserted directly behind the road and filled with a cell-by-cell copy of the source window, outlined in red. Rows landward of the block are pushed down the page by N. The measured pavement rows are outlined; the dark band is NC-12 where v3's 1984 setback places the model's road, the block directly behind it. The only seam is at the block's landward end, where its last row (a copy) meets the original first row. GIS 5 has no model road: the block goes in one row behind the crest row, and the window is the rows that follow it. Numbers per domain in `fill_copy_by_domain.csv`.

## `HAT_fill_copy_method_GIS<N>.png` (3-fill; GIS 80, 85, 5, 49)

The method in three stages at one domain, in the model's frame: the two dune rows on top (drawn at berm plus dune height), then the interior, 50 alongshore cells across, shaded by elevation class (m above MHW); NC-12 as a dark band at the two rows the model holds it. (a) The Barrier3D domain as extracted (v2), with the insert point marked one row behind the road. (b) The N rows the footprint identifies, inserted blank behind the road (hatched); everything landward is pushed down the page by N, the dune and the road do not move. (c) The same rows filled by a cell-by-cell copy of the N interior rows that follow them (dashed window, arrow), read from the built version v3; NC-12 drawn where v3's 1984 setback places the model's road, the block directly behind it, with the measured pavement rows outlined. GIS 5 has no model road: the block goes in one row behind the crest row, so the crest stays at the front and the copy takes what follows the block, exactly as behind the road.

## `HAT_fill_copy_grid_island.png` (3-fill)

(a–c) As `HAT_row_insert_grid_behindroad.png`, with every block filled by the copy rule instead of left blank: the N rows immediately landward of the insert point, copied cell by cell, so the block carries the same elevation classes as the rows that follow it. The replaced rows are outlined in red; removals stay hatched in blue. Dune rows on top; the dark band is NC-12 where v3's 1984 setback places the model's road, the block directly behind it, the measured pavement rows outlined; communities along the bottom. 29 domains gain 73 rows, 23 lose 47, 38 are unchanged.

## `HAT_footprint_v3_vs_v2_road.png` (4-result)

The 1984-2004 calibBE full-management hindcast on v3 (the footprint behind the road, copy fill, 1984 setbacks) against the same run on v2 (today's setbacks, floored at 0), at the road. (a) The setback each run starts with, per road domain, on a symmetric-log axis; v3's are the 1984 measurements and none is floored. (b) Every emergent relocation the model made, by year and domain; the recorded 1989 Pea Island and 1999 inter-village relocations drawn as green bars across their blocks, which are outlined. v2 relocates nine times, three of them in 1985 at GIS 85 and 86 by construction of the floor; v3 relocates seven times, none before 1988, and the Pea Island block fires in 1988 and 1995 against the 1989 record. Interior RMSE 0.547 (v2) and 0.544 (v3) m/yr; no road drowns in either.

## `HAT_method_compare_v2_v3_GIS85.png` (4-result)

GIS 85 under the two methodologies, in the model's frame and on the SAME pick set: the two dune rows on top (berm plus dune height), the interior below in elevation classes (m above MHW), cells on the left axis and metres landward of interior row 0 on the right. (a) v2, as extracted: the 1984 road measured against row 0 came out 15 m seaward of it, in the dune (dashed), and was floored to 0, so the model placed NC-12 on rows 0 and 1 at the dune (dark band); no rows added. (b) v3: the same extraction with five rows inserted directly behind the road and filled by copying the five rows that follow them (red outline), standing for the 59 m the dune line retreated between 1984 and 1997 in whole cells, and the road at its 1984 setback of 44 m, which puts the model's NC-12 on rows 4 and 5, measured cells, with the block directly behind it. Because the two share the pick set, everything else is cell-for-cell identical; the panels differ only by the block and the setback. The box in each panel gives the interior depth, the setback the model received, the road rows, and what the hindcast did with the road with prescribed relocations off (v2: the calibration-tree run, relocated 1985, 1991 and 2002; v3: arm behindroad-copy, relocated 1995) against the recorded 1989 relocation.

## `HAT_method_compare_v1_v3_GIS85.png` (4-result)

As the previous figure, with the ORIGINAL extraction v1 as the base (`--base v1`), so the re-pick is part of the difference. GIS 85 under the two methodologies, in the model's frame: the two dune rows on top (berm plus dune height), the interior below in elevation classes (m above MHW), cells on the left axis and metres landward of interior row 0 on the right. (a) v1, the original extraction: the 1984 road measured against row 0 came out 10 m seaward of it, in the dune (dashed), and was floored to 0, so the model placed NC-12 on rows 0 and 1 at the dune (dark band); no rows were added. (b) v3: the re-pick base with five rows inserted directly behind the road and filled by copying the five rows that follow them (red outline), standing for the 59 m the dune line retreated between 1984 and 1997 in whole cells, and the road at its 1984 setback of 44 m, which puts the model's NC-12 on rows 4 and 5, measured cells, with the block directly behind it; the measured position on this pick set, 15 m seaward of row 0, is dashed. The box in each panel gives the interior depth, the setback the model received, the road rows, and what the hindcast did with the road with prescribed relocations off (v1: arm pea1989basenoreloc, relocated 1985, 1991 and 2002; v3: arm behindroad-copy, relocated 1995) against the recorded 1989 relocation. The dune rows differ between the panels because v3's base is the 2026-09-02 re-pick: v1's search window (cells 7–14, exclusive) pinned the crest one cell seaward of the real one, so v1's dune rows are the 3.4 m pick and its interior row 0 is the actual 4.8 m crest, whereas v2's window (11–17) catches the crest, its dune rows are 5.3 m above MHW on median, and its row 0 is v1's row 1 cell for cell. The same surface, shifted one cell in where the dune array ends and the interior begins.

## `HAT_footprint_1984_grid.png`

(a–c) The 90 Barrier3D domains as the model indexes them, 50 alongshore cells each, every cross-shore row down the page from the CURRENT interior row 0 at 0 on every domain (topography v2; interiors run to 189 rows, white is off the array, cells are not square; right axis in metres). Existing cells are shaded by elevation class (m above MHW), so the dune ridge, the backbarrier flat and the sound-side marsh are distinguishable. Red rows above 0 are the rows that would be added to bring row 0 to the 1984 dune line, blank because no fill has been chosen; blue hatching marks existing rows 0 to |N|−1 that would be removed where the island has prograded since 1984; the signed count is printed above each changed domain. The black tick is where interior row 0 ends up; the dark bar is NC-12 at its measured 1984 position (seaward edge, 20 m wide), which does not move, so its distance to the tick is the new setback. Communities and villages along the bottom of each panel, from the site configuration. 29 domains gain 73 rows and 23 lose 47; 38 are unchanged. N = trunc(median paired shift / 10 m), so a row appears only once a full cell of change is measured; the largest are +7 at GIS 80 and -5 at GIS 63.

## `HAT_footprint_1984_rows.png`

Rows per domain under the 10 m rule, positive where rows are added (the 1984 dune line lay seaward of the 1997 line) and negative where existing rows are removed, with the communities banded along the axis. 29 domains gain 73 rows and 23 lose 47; 38 are unchanged. N = trunc(median paired shift / 10 m), so a row appears only once a full cell of change is measured; the largest are +7 at GIS 80 and -5 at GIS 63.

## `HAT_footprint_1984_plan.png`

(a–c) The same footprint in plan view, in three equal-aspect panels of thirty domains, south at left, each showing its domain boxes (2000 × 500 m) whole. Grey relief is the 2009-2014-1996 DEM hillshaded at 10 m and carries no readable elevation. The 1984 (red) and 1997 (blue) dune lines, NC-12 in 1984 (dashed), and each domain box shaded by N, red where rows are added and blue where they are removed, labelled on the landward edge. The solid band is the same N at true scale, the 1997 dune line offset seaward (add) or landward (remove) by N × 10 m, so one row is a 10 m sliver that needs a zoom; the band is anchored on the 1997 line as the map proxy for the existing array's seaward edge, which itself sits about 19 m seaward of interior row 0. The gap between the band's outer edge and the 1984 line is the truncation to whole cells. Communities as a bracket in the ocean margin, the pier and groin as seaward marks; scale bar 500 m = 50 cells. 29 domains gain 73 rows and 23 lose 47; 38 are unchanged. N = trunc(median paired shift / 10 m), so a row appears only once a full cell of change is measured; the largest are +7 at GIS 80 and -5 at GIS 63.

## `HAT_footprint_1984_plan_behindroad.png`

As the seaward plan view, for the rows placed BEHIND NC-12 as the model places it under the 1984 setback: the true-scale band hangs off the landward edge of the road moved inland by the measured 1984–1997 dune-line shift (the 1984 centreline offset 10 m for a 20 m road, then by the shift) and runs landward by N × 10 m, red where rows are added behind the road and blue where backbarrier rows are removed there. Box shading, dune lines and NC-12 as before; domains without a model road (GIS 1–5, 8) hang the band landward of the 1997 line, behind the crest row. The strip from the dune crest through the road is untouched in this placement, and the model keeps today's setback. 29 domains gain 73 rows and 23 lose 47; 38 are unchanged. N = trunc(median paired shift / 10 m), so a row appears only once a full cell of change is measured; the largest are +7 at GIS 80 and -5 at GIS 63.

## `HAT_footprint_1984_shift.png`

Per domain, the median of the 50 paired per-profile differences between the 1997 and 1984 dune-line crossings (points, p10–p90 bars), positive where the 1984 line lies seaward, and the part of it the 10 m rule keeps as whole rows (filled bars, N × 10 m; red added, blue removed). Dashed guides at ±1 cell; points inside them become no rows, and the distance from a point to its bar is the truncation residual, always toward less change. Communities banded along the axis; the two NC-12 relocation blocks outlined. 29 domains gain 73 rows and 23 lose 47; 38 are unchanged. N = trunc(median paired shift / 10 m), so a row appears only once a full cell of change is measured; the largest are +7 at GIS 80 and -5 at GIS 63.

## `HAT_footprint_1984_setback.png`

For the 82 road domains, the NC-12 setback the model receives today (open circles, the v2 measurement floored at 0) and the setback from the new row 0 (filled, coloured by what happens to the domain, with the p10–p90 over the profiles), both in metres landward of interior row 0 on a symmetric-log axis. The new value is (road − row 0) + shift per profile, then the median, unrounded. GIS 85 and 86, floored at 0 today, become 44 and 12 m; GIS 16 falls to 1 m after losing four rows. No new setback is negative.
