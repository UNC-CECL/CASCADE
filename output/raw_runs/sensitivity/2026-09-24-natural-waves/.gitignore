# The run folders stay on disk only: this scenario's run name is ~85
# characters and sits twice in each file path, past Windows' 260, so git
# cannot index them. Committed: README.md, tables/, and each figure's PDF,
# caption and data CSV under figures/**/supporting/ (see README "What is in git").
baseline/
baseline_full_management/
wave_height/
high_angle/
asymmetry/
wave_period/
full_management_*/
grid_*/
crash_check*/
